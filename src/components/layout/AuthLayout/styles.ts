import { CSSProperties } from "react";

export const containerStyles: CSSProperties = {
    display: "flex",
    flexWrap: "wrap",
    height: "100vh",
    alignItems: "center",
}

export const wrapperContentStyles: CSSProperties = {
    flex: 1
}